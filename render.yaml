services:
  - type: web
    name: secure-p2p-signaling
    runtime: node
    rootDir: signaling_server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        value: utm-kltn-secret-2026-abc
      - key: NODE_ENV
        value: production
